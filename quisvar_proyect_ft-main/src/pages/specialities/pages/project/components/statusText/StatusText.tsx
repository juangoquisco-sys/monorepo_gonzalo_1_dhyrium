import TaskStatusText, {
  type TaskStatusTextProps,
} from '@/pages/specialities/components/taskStatusText/TaskStatusText';

export const StatusText = (props: TaskStatusTextProps) => (
  <TaskStatusText {...props} />
);

export default StatusText;
